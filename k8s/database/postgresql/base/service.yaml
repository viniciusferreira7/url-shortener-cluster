apiVersion: v1
kind: Service
metadata:
  name: postgres-data
  namespace: dev
spec:
  selector:
    postgresql: url-shortener-postgresql
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None